# Models - mongoose-aggregate-paginate-v2

- To manage our watchHistory in "user.models.js". We will use advance mongoose approach called "aggregate pipeline".
  - For this install:
  ```
  npm i mongoose-aggregate-paginate-v2
  ```


- 